1. •	How would you fix your code so that this issue is no longer present?
We would fix this issue by not actually allowing the user to run shell commands. We would search for videos by using the database instead.
